import { Box } from '@chakra-ui/react';

export default function ChatInput() {
  return (
    <Box as="form" mt={2} display="flex">
      {/* Input and send button will go here in the future */}
    </Box>
  );
}
